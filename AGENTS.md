# AGENTS.md

Conventions for AI agents (and humans) working in this repo. Generated by `/setup-matt-pocock-skills` on 2026-09-06.

## Agent skills

### Issue tracker

Issues, specs, and wayfinder tickets live in **GitHub Issues** on `pioneerAlone/realtime_interpreter`, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default 5-role vocabulary (`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`) — labels exist on the remote, mapped 1:1 in `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one root `CONTEXT.md` plus `docs/adr/` once `/domain-modeling` lazily creates them. See `docs/agents/domain.md`.

## Local-only reference

`realtime_interpreter_Minimal_Implementation/` (the A-channel Doubao s2s PoC) is **git-ignored** in this repo. It stays on disk as local reference material only. It will be published separately as `pioneerAlone/realtime_interpreter-poc` when that decision lands; until then, do not commit, reference from `docs/`, or copy code from it without explicit user approval.
