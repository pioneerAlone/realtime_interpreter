# AGENTS.md

Conventions for AI agents (and humans) working in this repo. Generated by `/setup-matt-pocock-skills` on 2026-09-06.

## Agent skills

### Issue tracker

Issues, specs, and wayfinder tickets live in **GitHub Issues** on this repo, accessed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

> Setup-time note (2026-09-06): the repo is **not yet a git repo**, `gh auth status` reports `HTTP 401` (token invalid), and `github.com` is unreachable from this host. The tracker is configured for GitHub; the wire is not yet up. See the top of `docs/agents/issue-tracker.md` for the three-blocker bring-up checklist.

### Domain docs

Single-context repo — one root `CONTEXT.md` plus `docs/adr/` once `/domain-modeling` lazily creates them. See `docs/agents/domain.md`.
